# AGENTS.md — LLMS_Bridge 项目治理

本文件适用于**所有在本仓库内工作的 agent harness**(Codex / Claude Code / Gemini / Qoder CN /
WorkBuddy / Qwen / 其它接入的 worker),以及人。

本项目的特殊性:它不是普通应用,而是**一个会去调度其它 harness 的 harness**。因此普通项目的
"代码风格"约束在这里是次要的,下面的**四条铁律**才是治理核心——因为它们管的是"谁有权让另一个
厂商的模型替你干活"。

---

## 一、铁律

### 1. 凭证:桥不得持有任何厂商密钥

- 桥(MCP server / adapter / 调度器)**不读取、不存储、不落盘、不转发**任何**云端厂商**的 API key、
  OAuth token、session cookie。此禁令**无例外**——不接受任何"技术上需要"的豁免。
- 一律**复用各 CLI 自身的登录态**(Codex 自身 auth、Claude OAuth、Gemini `oauth_creds.json` 等),
  桥只做 subprocess 派发。
- 禁止把密钥写进 `config.toml` / `.env` / adapter 源码 / 测试夹具 / 日志。
- 调试需要看凭证时**只打印是否存在,不打印内容**。

#### 1.1 补充条款:本地服务的 token

层级④ 需要访问宿主自己的 localhost 服务(如 Qoder CN 的 MCP router `127.0.0.1:57913`),
其 token 写在宿主自己的配置里(如 `~/.qoder-cn/mcp-router.json`)。这类 token **不属于**上款禁令
所指的"云端厂商密钥",允许**就地复用**,但必须同时满足五个条件:

1. **只读**——只读宿主自身配置里已存在的 token,不生成、不续期、不改写。
2. **不落地**——只在内存中使用;不复制到任何其它文件,不写日志,不进事件流,不回传给大脑。
3. **范围限定**——只允许用于访问该宿主的 `127.0.0.1` 服务。禁止同一 token 用于任何非本机地址。
4. **禁止越界**——绝不读取云端厂商 key(`ANTHROPIC_*` / `OPENAI_*` / 各家 OAuth token 等),
   即使它们与 localhost token 同处一个配置文件。**按字段白名单读,不整文件读。**
5. **审计**——每次读取写一条审计记录(时间、宿主、字段名、用途),**不记录 token 值**。

- 违反后果(两款共用):桥会成为全厂商凭证的集中失守点。这是本项目唯一"一失全失"的风险面。

### 2. 审批:跨厂商派发默认不信任

- 任务模型里 `approval` 是**必填字段**,没有默认值。缺字段 = 拒绝执行,不是"用缺省"。
- **默认禁止**这些模式,除非用户在当次任务中显式点名授权:
  - Codex `--dangerously-bypass-approvals-and-sandbox`
  - Qwen / Gemini 的 `yolo` / `--approval-mode yolo`
  - Claude 的 `skipDangerousModePermissionPrompt`
- 常规档位用**最小够用**:Codex `-s workspace-write`、Gemini/Qwen `--approval-mode auto-edit`、
  Claude 常规 permission mode。
- worker 的写权限**限制在自己的 worktree 内**,不允许写到仓库外或系统目录。
- 给用户的提示语里,若涉及绕过审批,必须明说"这将让 XX 模型在无人确认下改动你的磁盘"。

### 3. 隔离:并行 worker 一个 worktree

- 任何可能改动文件的 worker,必须先分配独立 git worktree,不允许两个 worker 共享工作目录。
- **已实现**(`src/worktree.ts`):写任务在 git 仓库下自动分配 `--detach` worktree,
  worker 的 cwd 被换成它,**源仓库不动**。
- **默认永远是"新建 + 隔离"**;三种形态由调用方显式选(2026-09-24 经用户确认放开):
  `reuseWorktreePath`(复用已有 worktree,仅写档;桥会先验"同仓库且非主工作区")、
  `allowUnisolatedWrite: true`(直接写 cwd,**git 仓库里也生效**)、什么都不给(新建 worktree)。
- 放弃隔离必须**显式**,且结果、ack、账本三处都标 `isolated: false` ——
  不许把"没隔离"混成"隔离了"。非 git 目录下的写任务**默认拒绝**。
- 只读档不分配 worktree;给它传 `reuseWorktreePath` 会被**明确拒绝并指路**(想读某个
  worktree 的快照就把 cwd 指过去),不许静默忽略,也不许给它标 `isolated: true`
  —— 只读 worker 仍能按绝对路径读到源仓库,标了就是过度宣称。
- worker 产出以 `diff` 事件回收,**不直接合并**。worktree 保留不删,路径在结果里回传。
- 容器目录 `.llms-bridge/` 写进仓库本地的 `.git/info/exclude`,不改版本化的 `.gitignore`
  (不该因为桥跑了一次就污染用户的 diff)。
- 未经用户确认,不得把 worker 的改动合并或推送到任何远端。

### 3.1 数据去向:仓库级云策略

- 只读档的 worker **照样会把仓库内容发给模型** —— "只读"约束的是改不改文件,与数据去向无关。
- 语义(`src/policy.ts`,读 `<仓库>/.llms-bridge/policy.json`,文件本身进 `info/exclude`):
  - 文件**不存在** = 不限制(不给所有既有仓库加一道默认拒绝);
  - 文件存在 = 白名单:`cloud.allowHarnesses` 非空则 harness 必须在名单里;名单为空则只放行
    **自报端点在本机**的 harness;
  - **端点未知一律按不可信处理**。全机只有 claude 自报 egress,所以"不自报"在空名单下会被拒 ——
    把"不知道"当"安全"是本项目最忌的静默降级;
  - 文件坏掉/键名写错 = **拒**(用户建它是为了限制,解析失败不该悄悄解除限制)。

### 4. 成本:每个任务都要有上限

- `budget.max_tokens` 与 `budget.max_wall_ms` 是任务必填项。
- 超限时**中断并如实报告**,不允许静默续跑、不允许偷偷换更贵的模型把任务"跑完"。
- 优先档位顺序:本地 Ollama → 便宜档 → 贵档。要升档必须说明理由。

---

## 二、证据纪律

本项目大量结论是"这台机器上装了/支持什么",这类断言**必须穷举验证**,不许按惯例猜。

- **断言"本机有/没有 X"之前**,至少交叉验证四类证据:安装目录(含非标准位置)、注册表
  (同时看 `HKLM\SOFTWARE\...` 与 `HKLM\SOFTWARE\WOW6432Node\...`,以及 `HKCU\...`)、PATH、进程。
  只探一处就下结论是本项目**已经栽过的坑**。
- 判据要用**能直接验证的事实**(文件在不在、命令输出是什么),不要用会随版本漂移的命名或组件 ID。
- **禁止编造版本号、能力清单、性能数字**。引用某个 harness 的版本或能力时,必须同时给出:
  - 核实日期
  - 核实命令或依据
- 本仓库中 `PLAN.md` 的"本机现状"一节带有核实日期;**引用前先重查**,版本会漂移。
- 区分"已验证不通过"与"我没法验证"。后者要显式写出来,不要含糊带过。
- 结论一旦写进已提交文档后发现是错的,**单独提交更正,并明确告诉用户"此前版本是错的"**。

---

## 三、与外部 harness 交互的约定

- 三层级语义不同(①ACP 可多轮、②双向流靠 `--fork-session` 开分支、③一次性无上下文),
  差异**收敛在 adapter 内部**,对外只暴露统一事件模型,不要让调度器逻辑分叉。
- 事件模型以 Claude 的 stream-json 为基准(`status` / `message` / `tool_call` / `tool_result` /
  `diff` / `usage` / `error` / `result`),其它厂商向它归一化。
- 每个 adapter 必须实现 `detect()`(版本探测),启动时校验;探测失败时**显式标记该 harness 不可用**,
  不允许静默降级到别的厂商。
- 不要自造私有协议。能 ACP 就 ACP,能 stream-json 就 stream-json,能用宿主自己的 MCP 面就用 MCP。
- **禁止硬编码端口**。所有本地端口(宿主 API、CDP)都是进程启动时分配的临时值,重启即变。
  adapter 必须从宿主自己的登记文件读(如 `~/.workbuddy/tencent-docs-engine.port`、
  `~/.qoder-cn/mcp-router.json` 的 `baseUrl`/`pid`),或从受管进程反查监听端口。
- 层级④(本地 API / CDP)是宿主**未公开接口**,按 best-effort 对待,**不承认为稳定契约**。
  上游一变即断属预期内;失败时显式报错并回落层级③或⑤,**不许假装成功**。
- 不要重复实现已有件:`cc-switch`(provider 切换)、`mcporter`(MCP 路由)是既有依赖。

---

## 四、代码与文档纪律

- 不写注释解释"这行在做什么";只写"为什么"——非显然的约束、绕过某个具体缺陷的原因、
  会让人意外的行为。
- 不为不可能发生的场景加错误处理。信任内部代码与框架保证,**只在系统边界(用户输入、外部 API、
  其它厂商 CLI 的返回)做校验**——那是本项目的校验重点,因为外部 CLI 的输出格式不受我们控制。
- 不加超出任务所需的抽象。适配器之间有三行相似代码,好过一个过早的基类。
- 不留半成品。宁可一个 milestone 少做一条路径,也不要留下"以后再补"的空壳。
- **不新增文档文件**,除非用户明确要求。规划在 `PLAN.md`,治理在本文件,不要另起第三份。

---

## 五、提交纪律

- **提交前 `npm test` 必须全绿**。默认套件零额度消耗;涉及真实派发的用例由
  `LLMS_BRIDGE_LIVE=1` 显式开启,不得把它变成默认。
- **改任何闸门(审批/预算/隔离/凭证)都必须同时补或改用例** —— 这些是项目的主要风险面,
  光靠人工验过不算数。
- 只在用户明确要求时提交。
- 提交前先 `git status` 看全量改动;不提交凭证、日志、worker 产物、`.scratch/`。
- 提交信息说清**为什么**,不复述 diff。
- 不推送远端,除非用户明确要求该次推送。
- 破坏性操作(删分支、reset --hard、force push、清理 worktree)一律先问。

---

## 六、当前状态

- 仓库建立于 2026-09-19,尚未有实现代码。
- 设计见 `PLAN.md`(2026-09-19 修订版):**五级集成模型** + **多宿主主脑**
  ——主脑是"你此刻正在用的那个会话",不是某个固定 harness。
- M1 是三级横截面各打通一条路径(ACP / stream-json / 一次性),尚未开始。
  M2 才把桥挂进多家宿主(Codex / Claude Code / Gemini / Qoder / WorkBuddy)。
- `PLAN.md` §8 列有 7 项尚未核实的事项,动手前先验。
- 铁律 1.1(本地服务 token)是 2026-09-19 经用户确认新增的条款,不是默认允许。